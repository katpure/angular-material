import { OnlynodirectiveDirective } from './onlynodirective.directive';

describe('OnlynodirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlynodirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
